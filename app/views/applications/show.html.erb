<h1><%= @application.name %></h1>
<h3>Address: <%= @application.address %></h3>
<h3>Home Description: <%= @application.home_description %></h3>
<h3>Status: <%= @application.status %></h3>
<br/>
<br/>

<h3>Add a pet to this application:</h3>
<%= form_with url: "/applications/#{@application.id}", method: :get, local: true do |f| %>
  <%= f.label :search %>
  <%= f.text_field :search %>
  <%= f.submit "Search" %>
<% end %>

<br/>
<% if params[:search].present? %>
  <% @pets.each do |pet| %>
    <%= pet.name %>
    <form action="/applications/<%=@application.id%>"
      <input type ="hidden" id="pet" name="pet" value=<%= "pet.id" %> />
      <input type ="submit" value="Adopt this pet">
    <br/>
  <% end %>
<% end %>